# Claude — McCluster authority first

You are in the McCluster ecosystem.

1. Read `AGENTS.md` before making architectural decisions.
2. `mcclusterishere/mccluster` is the authoritative McCluster ecosystem/master repository.
3. `mcclusterishere/Here` is retired legacy and is NEVER an authority, upstream, control plane, backend source of truth, deployment source, or architecture reference.
4. Level 3 Media may own the product-specific backend capabilities explicitly authorized in this repo's `AGENTS.md`. Do not collapse those capabilities merely because this product belongs to McCluster.
5. Worker/API names must follow current McCluster law: Worker `mccluster`, API `https://api.mccluster.org`; do not invent a Worker named `mccluster-core`.

If older Claude instructions conflict with `AGENTS.md`, `AGENTS.md` wins.
